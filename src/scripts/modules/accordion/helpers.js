export const cssClasses = Object.freeze({
    buttons: 'accordion__title',
    contents: 'accordion__content',
    arrows: 'accordion__title-arrow'
});

export const attributes = Object.freeze({
    ariaExpanded: 'aria-expanded'
});
